{
  "version": 2,
  "rootDirectory": "blx",
  "buildCommand": "yarn install && yarn build",
  "installCommand": "yarn install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "NODE_ENV": "production"
  }
}

